"""
Build
=====

This :mod:`pywincffi.build` package is used by pywincffi's build system.  It's
primary purpose is to consume events from GitHub, run builds and report back
to GitHub with results.

This Python package is not included as part of the distribution on PyPi.  The
package must be manually installed by setting ``PYWINCFFI_INSTALL_BUILD`` in
the environment prior to running the setup.py script.
"""
