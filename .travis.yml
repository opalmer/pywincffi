sudo: false
language: python
python:
  - 2.6
  - 2.7
  - 3.3
  - 3.4

env:
  - READTHEDOCS=1
  - PYLINT=1

before_install:
  - source .ci/travis/retry.sh
  - retry .ci/travis/before_install.sh

script:
  - .ci/travis/script.sh
