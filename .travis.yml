sudo: false
language: python
python:
  - 2.6
  - 2.7
  - 3.3
  - 3.4

matrix:
  exclude:
    - python: 2.6
      env: PYLINT=1

env:
  - READTHEDOCS=1
  - PYLINT=1

install:
  - source .ci/travis/retry.sh
  - retry .ci/travis/install.sh

script:
  - .ci/travis/script.sh
